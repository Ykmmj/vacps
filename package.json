{
  "name": "vps-agent-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare control plane and VPS-resident task agents.",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=22.14.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm format:check && pnpm typecheck && pnpm test",
    "dev:control": "pnpm --filter @vps-agent/control-worker dev",
    "dev:agent": "pnpm --filter @vps-agent/vps-agent dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "setup:cloudflare": "node scripts/setup-cloudflare.mjs",
    "test": "pnpm -r test",
    "typecheck": "pnpm --filter @vps-agent/contracts build && pnpm -r typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "@types/node": "^26.1.1",
    "eslint": "^9.29.0",
    "prettier": "^3.5.3",
    "prettier-plugin-svelte": "^4.1.1",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.34.0"
  }
}
